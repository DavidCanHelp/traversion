// Test file for Traversion - Now with more vibes! ✨
console.log('Hello, vibe coders!');

function greet(name) {
  return `Hey ${name}, ready to traverse time?`;
}

// Adding some async vibes
async function fetchVibes() {
  return new Promise(resolve => {
    setTimeout(() => resolve('Good vibes detected! 🎵'), 1000);
  });
}